# Database

The backend uses Prisma as ORM and PostgreSQL as database.

Prisma schema location:

```text
apps/api/prisma/schema.prisma
```

The schema defines the application models and is used to generate the Prisma client.
